package com.example.sprintserver.sprint.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface SprintFieldMaxRepository extends JpaRepository<SprintFieldCount, Long> {
}
